import styled from 'styled-components/native';

const HomeLayOut = styled.ScrollView`
  width: 100%;
  display: flex;
  padding: 20px;
  background: #ffffff;
`;

const TitleText = styled.Text`
  font-size: 18px;
`;

export {HomeLayOut, TitleText};
