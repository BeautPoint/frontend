import styled from 'styled-components/native';

const Layout = styled.View`
  display: flex;
  width: 100%;
  height: 100%;
`;

const Overlay = styled.Pressable`
`;

const MapMarker = styled.Pressable`
`;

export {Layout, Overlay, MapMarker};
